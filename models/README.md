# Models

This package contains data models used for Wails bindings.

These types are thin wrappers around config types, providing the necessary JSON marshaling/unmarshaling for Wails frontend communication.

